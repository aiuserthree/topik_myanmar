-- TOPIK Myanmar — PRODUCTION seed (reference data only)
-- =============================================================================
-- 운영 DB 초기 1회 적용. 비밀이 아닌 "참조 데이터"만 포함합니다.
--   · 데모 회원 / 데모 관리자 / 약한 비밀번호  → 절대 포함하지 않음
--   · 관리자 계정          → `npm run create-admin` 으로 별도 생성 (bcrypt, 강한 비번)
--   · 시험장(exam_venues)  → BO 화면 "시험장 관리"에서 생성
--   · 회차(exam_rounds)    → BO 화면 "회차 관리"에서 생성
--
-- 여기서 시드하는 것: 13자리 수험번호 채번에 필요한 국가/지역 코드뿐입니다.
-- (수험번호 = 국가(3)+지역(3)+수준(1)+시험장(2)+응시자(4). exam_venues 가 이
--  country_region_codes 를 FK 로 참조하므로, 시험장 생성 전에 지역 코드가 있어야 합니다.)
--
-- 직업/응시동기/응시목적 코드는 DB 테이블이 아니라 애플리케이션 상수입니다
-- (api/src/lib/admin-helpers.ts, html/shared/roster-codes.js) — 시드 불필요.
--
-- 재실행 안전: ON CONFLICT DO NOTHING.
-- 적용: `npm run seed:prod`  또는  `node scripts/migrate.js --prod-seed-only`
-- =============================================================================

BEGIN;

-- ---------------------------------------------------------------------------
-- 국가/지역 코드 — 미얀마(국가코드 025) 주요 지역
-- region_code 는 13자리 수험번호의 ② 지역코드(3자리)로 사용됩니다.
-- 신규 지역이 필요하면 같은 형식으로 INSERT 하거나 BO 확장 시 추가하세요.
-- ---------------------------------------------------------------------------
INSERT INTO country_region_codes (country_code, region_code, name_ko, name_en) VALUES
    ('025', '001', '양곤',     'Yangon'),
    ('025', '002', '만달레이', 'Mandalay'),
    ('025', '003', '네피도',   'Naypyidaw'),
    ('025', '004', '몽유와',   'Monywa'),
    ('025', '005', '바고',     'Bago'),
    ('025', '006', '타웅지',   'Taunggyi')
ON CONFLICT (country_code, region_code) DO NOTHING;

COMMIT;

-- =============================================================================
-- 운영 첫 적용 후 다음 순서로 진행하세요:
--   1) npm run create-admin -- --email <운영자메일> --name "<이름>"  (비번 입력)
--   2) BO 로그인 → 시험장 관리에서 실제 시험장 등록 (venue_code 2자리, 지역 선택)
--   3) BO 회차 관리에서 실제 회차 등록 (접수기간/응시료/정원/시험장 매핑)
--   4) BO 공지/FAQ 관리에서 공지·FAQ 등록 및 발행
-- =============================================================================
