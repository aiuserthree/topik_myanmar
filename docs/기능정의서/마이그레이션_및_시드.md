# 마이그레이션 및 시드 (v0.1)

**작성일:** 2026-06-02  
**상태:** 초기 스키마·dev 시드 — 백엔드 착수용  
**근거:** `DB스키마_초안.md`, `개발자_체크리스트.md` NO.158–159, C안 BO `assets/data.js`

---

## 1. 마이그레이션 도구 선택

| 항목 | 결정 |
|------|------|
| **도구** | **Flyway** (Community) |
| **DB** | **PostgreSQL 15+** (`DB스키마_초안.md` 권장) |
| **형식** | 순차 SQL 파일 `db/migrations/V{nnn}__description.sql` |
| **대안** | Liquibase (XML/YAML) — 팀에 DB 변경 리뷰 자동화가 필요할 때; v0.1은 SQL 단순성 우선 |

### Flyway 선택 이유

1. **SQL-first** — 스키마 초안이 이미 PostgreSQL DDL로 표현 가능.
2. **버전 추적** — `flyway_schema_history`로 dev/prod 적용 이력 분리 (`배포_아키텍처.md` §4 dev/prod 2환경).
3. **CI 연동** — `flyway migrate` 한 줄로 파이프라인에 삽입 용이 (백엔드 착수 후).
4. **Alembic 제외** — 런타임은 **Node + Drizzle**(`백엔드_스택_결정.md`); DDL은 Flyway만 유지.

### 버전 관리 전략

| 규칙 | 내용 |
|------|------|
| 파일명 | `V001__initial_schema.sql`, `V002__add_foo.sql` — **한 번 적용된 파일은 수정 금지** |
| 롤백 | Flyway Community는 down migration 미지원 → **수정은 새 `V00n` forward migration** |
| 환경 | dev·prod **동일 migration 경로**, DB 인스턴스만 분리 (체크 233) |
| 시드 | Flyway **외부** — `db/seed/dev_seed.sql` 수동/스크립트 (prod 시드 없음) |

---

## 2. 폴더 구조

```text
db/
├── migrations/
│   └── V001__initial_schema.sql    # core tables (v0.1)
├── seed/
│   └── dev_seed.sql                # dev master + demo accounts
└── README.md                       # 로컬 실행 방법
```

---

## 3. V001 스키마 범위

`DB스키마_초안.md` §4 기준 **핵심 테이블**:

| 테이블 | § | 비고 |
|--------|---|------|
| `users` | 4.1 | FO 회원 |
| `exam_rounds` | 4.2 | 회차 마스터 |
| `country_region_codes` | 4.3 | 025/001 등 |
| `exam_venues` | 4.4 | 시험장 2자리 코드 |
| `exam_round_venues` | 4.5 | 회차–시험장 N:M |
| `application_submissions` | 4.6 | 동시 접수 그룹 |
| `applications` | 4.7 | 급수별 접수 행 |
| `exam_number_sequences` | 4.8, 4.18 | 채번 원자성 |
| `admin_users` | 4.9 | BO 계정 |
| `admin_audit_logs` | 4.10 | 처리 이력 |
| `notices` | 4.11 | 공지 |
| `board_posts` | 4.12 | 환불·문의 |
| `board_comments` | 4.13 | |
| `faq_items` | 4.14 | |
| `terms` / `term_agreements` | 4.15 | |
| `file_attachments` | 4.16 | |
| `email_outbox` | 4.17 | |
| `email_verification_codes` | 4.18 | |
| `password_reset_tokens` | 4.18 | |
| `user_sessions` | 4.18 | |
| `notice_view_logs` | 4.18 | |

**미포함 (정책 합의 후):** `application_drafts` (체크 466), admin 세션 전용 테이블.

---

## 4. Dev 시드 (`db/seed/dev_seed.sql`)

| 데이터 | 출처 |
|--------|------|
| 국가·지역 코드 025/001–004 | C안 BO `REGIONS` |
| 시험장 01–05 | C안 BO `VENUES` |
| 제106회·105회 회차 | C안 BO `SESSIONS` + 정책 워크시트 응시료 50,000/75,000 MMK |
| 약관 v1.0 초안 | placeholder 본문 |
| Super admin 1계정 | `admin-dev@topik-mm.local` — **bcrypt, prod 교체 필수** (체크 276, 378) |
| Demo FO user | `demo@topik-mm.local` — **prod 금지** |
| 공지 1건 | 제106회 접수 안내 |
| `exam_number_sequences` | 106회 × 활성 시험장 × Ⅰ/Ⅱ |

---

## 5. 트랜잭션·백업 (체크 160–161 연계)

| 항목 | v0.1 방침 |
|------|-----------|
| **접수 유실 방지** | 접수 제출·수납·수험번호 부여는 **단일 DB 트랜잭션** + `applications.rev` 낙관적 잠금 (API 409) |
| **채번** | `exam_number_sequences` + `SELECT … FOR UPDATE` (스키마 §4.8) |
| **백업** | prod: 일 1회 풀 백업 + WAL (호스팅 RDS 등) — **운영 합의 후** `배포_아키텍처.md` 갱신 |
| **복구 RTO/RPO** | 미정 — 인프라 프로비저닝 시 확정 |

---

## 6. 실행

로컬: `db/README.md` 참고.

```bash
createdb topik_mm_dev
psql -d topik_mm_dev -f db/migrations/V001__initial_schema.sql
psql -d topik_mm_dev -f db/seed/dev_seed.sql
```

---

## 7. 관련 문서

| 문서 | 경로 |
|------|------|
| DB 스키마 초안 | `DB스키마_초안.md` |
| ERD·시퀀스 플로우 | `ERD_및_플로우.md` |
| 배포·DB 분리 | `배포_아키텍처.md` |
| 백엔드 스택 | `백엔드_스택_결정.md` |
| 정책 합의 | `정책_합의_워크시트.md` |
| 로컬 실행 | `../../db/README.md` |

---

## 8. 변경 이력

| 버전 | 일자 | 내용 |
|------|------|------|
| v0.1 | 2026-06-02 | Flyway 선택, V001 스키마, dev 시드 — 백로그 #7 |
| v0.1 | 2026-06-03 | `ERD_및_플로우.md` 링크 추가 — 백로그 #9 |
| v0.1 | 2026-06-03 | `백엔드_스택_결정.md` 링크 — Flyway·Node 정렬 |
